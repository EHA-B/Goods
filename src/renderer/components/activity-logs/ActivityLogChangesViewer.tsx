import { Card } from "../ui";
import { stringifyValue } from "../../pages/activity-logs/activityLogsUtils";
function Values({ title, values }:{title:string; values?:Record<string,unknown>|null}){ const entries=Object.entries(values ?? {}); return <Card header={title}>{entries.length ? <div className="space-y-3">{entries.map(([key,value])=><div key={key} className="flex items-start justify-between gap-4 border-b border-[var(--border)] pb-2 last:border-0 last:pb-0"><span className="text-xs font-bold text-[var(--text-muted)]">{key}</span><span dir="auto" className="max-w-[65%] break-words text-left text-sm text-[var(--text-primary)]">{stringifyValue(value)}</span></div>)}</div> : <p className="text-sm text-[var(--text-muted)]">لا توجد بيانات.</p>}</Card>; }
export default function ActivityLogChangesViewer({oldData,newData}:{oldData?:Record<string,unknown>|null;newData?:Record<string,unknown>|null}){ return <div className="grid gap-5 lg:grid-cols-2"><Values title="القيم قبل العملية" values={oldData}/><Values title="القيم بعد العملية" values={newData}/></div>; }
