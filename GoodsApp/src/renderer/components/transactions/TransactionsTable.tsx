import { Eye, Pencil, Trash2 } from "lucide-react";
import DataTable from "../common/DataTable";import DataTableBody from "../common/DataTableBody";import DataTableCell from "../common/DataTableCell";import DataTableHead from "../common/DataTableHead";import DataTableHeaderCell from "../common/DataTableHeaderCell";import DataTableRow from "../common/DataTableRow";import { Button } from "../ui";import type { FinancialTransaction } from "./types";import TransactionTypeBadge from "./TransactionTypeBadge";
const money=(v:number)=>new Intl.NumberFormat("en-US",{minimumFractionDigits:2,maximumFractionDigits:2}).format(v);
export default function TransactionsTable({items,onView,onEdit,onDelete}:{items:FinancialTransaction[];onView:(x:FinancialTransaction)=>void;onEdit:(x:FinancialTransaction)=>void;onDelete:(x:FinancialTransaction)=>void}){return <DataTable><DataTableHead><DataTableRow>{["التاريخ","النوع","الفئة","الصندوق","الوصف","المرجع","المبلغ","الإجراءات"].map(h=><DataTableHeaderCell key={h}>{h}</DataTableHeaderCell>)}</DataTableRow></DataTableHead><DataTableBody>{items.map(x=><DataTableRow key={x.id}><DataTableCell>{x.transactionDate}</DataTableCell><DataTableCell><TransactionTypeBadge type={x.direction}/></DataTableCell><DataTableCell className="font-medium text-[var(--text-primary)]">{x.categoryName}</DataTableCell><DataTableCell>{x.cashboxName}</DataTableCell><DataTableCell>{x.description||"—"}</DataTableCell><DataTableCell><span dir="ltr" className="tabular-nums">{x.referenceNumber||"—"}</span></DataTableCell><DataTableCell><span dir="ltr" className="font-bold tabular-nums">{money(x.amount)} <span className="mr-1 text-xs font-medium text-[var(--text-muted)]">ل.س</span></span></DataTableCell><DataTableCell><div className="flex items-center gap-2"><Button size="sm" variant="secondary" startIcon={<Eye size={15}/>} onClick={()=>onView(x)}>استعراض</Button><Button size="sm" variant="secondary" startIcon={<Pencil size={15}/>} onClick={()=>onEdit(x)}>تعديل</Button><Button size="sm" variant="danger" startIcon={<Trash2 size={15}/>} onClick={()=>onDelete(x)}>حذف</Button></div></DataTableCell></DataTableRow>)}</DataTableBody></DataTable>}
